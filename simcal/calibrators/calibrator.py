class Calibrator(object):
    def calibrate(self, evaluate_point, compute_loss, reference_data):
        pass
        # result=evaluate_point(calibration)
        # compute_loss(reference_data,result)

    def add_param(self, name, postfix=""):
        pass  # return new param
